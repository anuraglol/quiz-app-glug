{
  "name": "server",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "@quiz-app/db": "workspace:*",
    "better-auth": "^1.2.5",
    "hono": "^4.11.7"
  },
  "devDependencies": {
    "@types/node": "^22",
    "wrangler": "^4.4.0"
  }
}
